# v1.1 – načrt (po MVP)

**Status: ⬜ načrt (17. 7. 2026).** MVP v1.0 je zaključen; to je predlog
obsega za naslednjo iteracijo, po prioriteti.

## 1. Izdaja na Google Play (najprej!)

* [x] `applicationId` → `com.romanev.veggiemama` (Play zavrne com.example)
* [x] signing prek `android/key.properties` (gitignoriran; fallback na debug)
* [ ] **keystore ustvari uporabnik** (enkrat, varno shrani + backup):
  ```powershell
  keytool -genkey -v -keystore $env:USERPROFILE\veggiemama-release.keystore -alias veggiemama -keyalg RSA -keysize 2048 -validity 10000
  ```
  nato kopiraj `android/key.properties.example` → `android/key.properties` in izpolni.
* [ ] AAB: `flutter build appbundle --release` (deluje, 41.8 MB)
* [ ] Play Console: interno preizkušanje (glej greenheart skilla
  `play-release-internal` in `play-console-content`), zasebnostna politika
  (podatki ostajajo na napravi — enostavna), content rating, store listing
  (checklist v design-guidelines §8)

## 2. Vsebinski sistem za prevode

* [ ] recepti, katalog živil in empatična sporočila iz kode v
  `assets/content/{sl,en,de}/*.json`; repozitoriji berejo po aktivnem jeziku
* [ ] prevodi en/de za vso vsebino (ton mora preživeti prevod!)

## 3. Onboarding dopolnitev (odloženo iz Faze 6)

* [ ] `UserModel`: faza dojenja, prehranske preference, cilji
* [ ] korak »cilji« v onboardingu + prikaz na Profilu

## 4. Mikrointerakcije in vizualni piš

* [ ] Lottie animacije: odklep dosežka, quick add za vodo, prazna stanja
* [ ] ilustracije za prazna stanja (namesto ikon)

## 5. AI izboljšave

* [ ] prijaznejše sporočilo ob 429 (free tier RPM) z avtomatskim retry po 60 s
* [ ] shranjevanje pogovorov v Hive (zdaj samo v pomnilniku)
* [ ] osebni kontekst v promptu (faza uporabnice, starost otroka — s privolitvijo)

## 6. Rast otroka (v1.1+ iz ARCHITECTURE)

* [ ] rastne krivulje/percentili — informacija, ne ocena; nežen prikaz
